AI SCAM DETECTOR:

How to Run:

1. install all the requiremenst listed in the requirements.txt.
2. Then open the backend folder  --> create a new .env file and copy the code from the .env.example and paste it in the .env file.
3. Then open the backend folder in terminal and type --->  python app.py. Now it will show the lost host server and click the server to check whether the website shows an json text. if so the app.py is working.
4. Then open the fronend folder in terminal and type statrd cd "the location of the index.html file ". or else open the index.html file directly in the folder.

Now It will show like this.

<img width="1720" height="863" alt="image" src="https://github.com/user-attachments/assets/85ae9a7e-3655-439b-be79-2959f8a2fe71" />
